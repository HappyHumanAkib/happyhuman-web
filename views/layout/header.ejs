<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>HappyHuman-সুখীমানুষ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar { background-color: #f8f9fa; border-bottom: 2px solid #28a745; }
        .welcome-text { text-align: center; padding: 10px; background: #e9ecef; border-radius: 5px; margin: 10px 0; }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container">
        <a class="navbar-brand fw-bold text-success" href="/">HappyHuman</a>
        <div class="ms-auto">
            <a href="/" class="btn btn-sm btn-link">Home</a>
            <% if (user) { %>
                <a href="/add" class="btn btn-sm btn-success">Share Content</a>
                <a href="/auth/logout" class="btn btn-sm btn-outline-danger">Logout</a>
            <% } else { %>
                <a href="/auth/login" class="btn btn-sm btn-primary">Login</a>
                <a href="/auth/register" class="btn btn-sm btn-outline-primary">Join Us</a>
            <% } %>
        </div>
    </div>
</nav>